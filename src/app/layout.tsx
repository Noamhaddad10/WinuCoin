// Root layout — minimal pass-through.
// Full HTML structure (html, body, fonts, providers) is defined in [locale]/layout.tsx.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
